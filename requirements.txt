# Обработка данных
polars>=1.18.0
duckdb>=1.5.0
pandas>=3.0.0
pyarrow>=18.0.0

# Скачивание датасета
huggingface-hub>=0.27.0

# Графы
networkx>=3.4.0
python-louvain==0.16

# Машинное обучение
scikit-learn>=1.6.0
numpy>=2.0.0

# Визуализация
matplotlib>=3.9.0
seaborn>=0.13.0
adjustText>=1.3.0      # task3/task6: непересекающиеся подписи узлов графа
umap-learn>=0.5.12     # task6: UMAP-проекция артистов по аудио-эмбеддингам
numba>=0.60            # transitive для umap-learn — фиксируем чтобы не зависеть от резолвера
llvmlite>=0.43         # transitive для numba
pynndescent>=0.5       # transitive для umap-learn (kNN)
psutil>=7.2.0          # task6: контроль RSS между DuckDB-коннектами

# Прогресс-бары
tqdm>=4.67.0

# Jupyter (для экспериментов)
jupyter>=1.1.0
ipykernel>=6.0.0
